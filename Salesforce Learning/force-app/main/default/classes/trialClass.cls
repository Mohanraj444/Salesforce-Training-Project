public class trialClass {
	public void addInteger(Integer a, Integer b)
    {
        Integer output = a + b;
        System.debug('Added output of two added numbers ' + output);
    }
    public static void addIntegers(Integer a, Integer b)
    {
        Integer output = a + b;
        System.debug('Added output of two added numbers ' + output);
    }
}